# EduNexus管理员端 - 后端接口文档

## 基础信息

- **基础URL**: `https://api.edunexus.com/admin`
- **API版本**: `v1`
- **数据格式**: JSON
- **字符编码**: UTF-8

## 认证方式

所有接口（除登录外）需要在请求头中携带Token：

```
Authorization: Bearer {token}
```

---

## 1. 认证模块

### 1.1 管理员登录

**接口地址**: `POST /admin/auth/login`

**请求参数**:
```json
{
  "username": "admin",      // 管理员用户名
  "password": "admin123"    // 管理员密码
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expires_in": 7200,
    "admin": {
      "id": 1,
      "username": "admin",
      "email": "admin@edunexus.com",
      "role": "super_admin",
      "permissions": ["user_management", "system_config", "audit_management"]
    }
  }
}
```

---

### 1.2 退出登录

**接口地址**: `POST /admin/auth/logout`

**响应示例**:
```json
{
  "code": 200,
  "message": "退出成功"
}
```

---

### 1.3 获取当前管理员信息

**接口地址**: `GET /admin/auth/me`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "username": "admin",
    "email": "admin@edunexus.com",
    "role": "super_admin",
    "permissions": ["user_management", "system_config", "audit_management"],
    "last_login_time": "2024-01-15 10:30:00",
    "login_count": 156
  }
}
```

---

## 2. 数据看板模块

### 2.1 获取平台统计概览

**接口地址**: `GET /admin/dashboard/stats`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total_users": 1568,          // 总用户数
    "total_enterprises": 234,     // 企业数
    "total_universities": 89,     // 高校数
    "total_cooperations": 567,    // 合作项目数
    "today_logins": 128,          // 今日登录数
    "pending_audits": 15,         // 待审核数
    "active_users": 342,          // 活跃用户数
    "system_health": "excellent"  // 系统健康状态
  }
}
```

---

### 2.2 获取用户增长趋势

**接口地址**: `GET /admin/dashboard/user-growth`

**请求参数**:
- `period` (string, 可选): 时间周期，month-按月，week-按周，默认month
- `months` (int, 可选): 查询月数，默认6

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "labels": ["1月", "2月", "3月", "4月", "5月", "6月"],
    "enterprise_users": [120, 132, 101, 134, 90, 230],
    "university_users": [80, 95, 120, 145, 110, 180],
    "total_users": [200, 227, 221, 279, 200, 410]
  }
}
```

---

### 2.3 获取合作项目统计

**接口地址**: `GET /admin/dashboard/cooperation-stats`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "by_status": {
      "pending": 8,      // 待审核
      "approved": 5,     // 已通过
      "rejected": 2      // 已拒绝
    },
    "by_type": {
      "talent": 12,      // 人才培养
      "course": 8,       // 课程共建
      "internship": 5,   // 实习基地
      "research": 3      // 科研合作
    },
    "success_rate": 85.7  // 合作成功率
  }
}
```

---

### 2.4 获取系统性能数据

**接口地址**: `GET /admin/dashboard/system-performance`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "cpu_usage": 45.2,      // CPU使用率
    "memory_usage": 67.8,   // 内存使用率
    "disk_usage": 32.1,     // 磁盘使用率
    "response_time": 128,   // 平均响应时间(ms)
    "uptime": "15天8小时",  // 系统运行时间
    "api_calls": {
      "today": 15680,       // 今日API调用
      "yesterday": 14230    // 昨日API调用
    }
  }
}
```

---

## 3. 用户管理模块

### 3.1 获取用户列表

**接口地址**: `GET /admin/users`

**请求参数**:
- `type` (string, 可选): 用户类型：enterprise-企业用户，university-高校用户
- `keyword` (string, 可选): 用户名或邮箱关键词
- `status` (int, 可选): 状态：0-禁用，1-启用
- `page` (int, 可选): 页码，默认1
- `size` (int, 可选): 每页数量，默认10

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "username": "tech_company",
        "email": "contact@tech.com",
        "type": "enterprise",
        "company_name": "XX科技有限公司",
        "status": 1,
        "created_at": "2024-01-10 14:30:00",
        "last_login_time": "2024-01-15 09:20:00"
      },
      {
        "id": 2,
        "username": "tsinghua",
        "email": "admin@tsinghua.edu.cn",
        "type": "university",
        "university_name": "清华大学",
        "status": 1,
        "created_at": "2024-01-08 10:15:00",
        "last_login_time": "2024-01-14 16:45:00"
      }
    ],
    "total": 1568,
    "page": 1,
    "size": 10
  }
}
```

---

### 3.2 获取用户详情

**接口地址**: `GET /admin/users/{id}`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "username": "tech_company",
    "email": "contact@tech.com",
    "type": "enterprise",
    "company_name": "XX科技有限公司",
    "phone": "010-12345678",
    "status": 1,
    "created_at": "2024-01-10 14:30:00",
    "updated_at": "2024-01-12 09:15:00",
    "last_login_time": "2024-01-15 09:20:00",
    "login_count": 45,
    "profile": {
      "industry": "互联网",
      "scale": "中型企业",
      "location": "北京市"
    }
  }
}
```

---

### 3.3 启用/禁用用户

**接口地址**: `PUT /admin/users/{id}/status`

**请求参数**:
```json
{
  "status": 0  // 0-禁用，1-启用
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "用户状态已更新"
}
```

---

### 3.4 重置用户密码

**接口地址**: `POST /admin/users/{id}/reset-password`

**响应示例**:
```json
{
  "code": 200,
  "message": "密码重置成功",
  "data": {
    "new_password": "Abc123456"  // 临时密码
  }
}
```

---

## 4. 合作审核模块

### 4.1 获取待审核合作列表

**接口地址**: `GET /admin/cooperation-audits`

**请求参数**:
- `status` (string, 可选): 审核状态：pending-待审核，approved-已通过，rejected-已拒绝
- `type` (string, 可选): 合作类型：talent-人才培养，course-课程共建，internship-实习基地，research-科研合作
- `keyword` (string, 可选): 企业或高校名称关键词
- `page` (int, 可选): 页码，默认1
- `size` (int, 可选): 每页数量，默认10

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "enterprise_name": "XX科技有限公司",
        "university_name": "清华大学",
        "type": "course",
        "project_name": "Java企业级开发课程共建",
        "apply_time": "2024-01-15 10:30:00",
        "status": "pending",
        "applicant": "张经理",
        "contact_phone": "13800138000"
      }
    ],
    "total": 15,
    "page": 1,
    "size": 10
  }
}
```

---

### 4.2 获取合作审核详情

**接口地址**: `GET /admin/cooperation-audits/{id}`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "enterprise_id": 1,
    "enterprise_name": "XX科技有限公司",
    "university_id": 1,
    "university_name": "清华大学",
    "type": "course",
    "project_name": "Java企业级开发课程共建",
    "description": "与企业合作开展Java企业级开发课程共建项目...",
    "goals": "培养符合企业需求的Java开发人才...",
    "start_time": "2024-02-01",
    "end_time": "2024-06-30",
    "expected_students": 50,
    "apply_time": "2024-01-15 10:30:00",
    "status": "pending",
    "applicant": "张经理",
    "contact_phone": "13800138000",
    "attachments": [
      {
        "id": 1,
        "name": "合作方案.pdf",
        "url": "https://example.com/files/cooperation.pdf",
        "size": 2048000
      }
    ]
  }
}
```

---

### 4.3 审核合作申请

**接口地址**: `POST /admin/cooperation-audits/{id}/review`

**请求参数**:
```json
{
  "result": "approved",        // 审核结果：approved-通过，rejected-拒绝
  "comment": "string",          // 审核意见，必填
  "suggestions": "string"       // 改进建议，可选
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "审核完成",
  "data": {
    "id": 1,
    "status": "approved",
    "reviewer": "管理员",
    "review_time": "2024-01-16 14:20:00"
  }
}
```

---

## 5. 系统管理模块

### 5.1 获取系统配置

**接口地址**: `GET /admin/system/config`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "site_name": "EduNexus",
    "site_description": "校企合作平台",
    "admin_email": "admin@edunexus.com",
    "max_file_size": 10485760,
    "allowed_file_types": ["pdf", "doc", "docx", "jpg", "png"],
    "session_timeout": 7200,
    "registration_enabled": true,
    "maintenance_mode": false,
    "api_rate_limit": 1000
  }
}
```

---

### 5.2 更新系统配置

**接口地址**: `PUT /admin/system/config`

**请求参数**:
```json
{
  "site_name": "EduNexus",
  "site_description": "校企合作平台",
  "admin_email": "admin@edunexus.com",
  "max_file_size": 10485760,
  "session_timeout": 7200,
  "registration_enabled": true,
  "maintenance_mode": false
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "系统配置已更新"
}
```

---

### 5.3 获取系统日志

**接口地址**: `GET /admin/system/logs`

**请求参数**:
- `level` (string, 可选): 日志级别：info, warning, error
- `module` (string, 可选): 模块名称：auth, user, cooperation, system
- `start_time` (string, 可选): 开始时间，格式：YYYY-MM-DD HH:mm:ss
- `end_time` (string, 可选): 结束时间，格式：YYYY-MM-DD HH:mm:ss
- `page` (int, 可选): 页码，默认1
- `size` (int, 可选): 每页数量，默认20

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "level": "info",
        "module": "auth",
        "message": "用户登录成功",
        "username": "tech_company",
        "ip": "192.168.1.100",
        "timestamp": "2024-01-15 09:20:00"
      },
      {
        "id": 2,
        "level": "warning",
        "module": "cooperation",
        "message": "合作申请提交失败",
        "username": "tsinghua",
        "ip": "192.168.1.101",
        "timestamp": "2024-01-15 10:30:00"
      }
    ],
    "total": 15680,
    "page": 1,
    "size": 20
  }
}
```

---

### 5.4 清理系统缓存

**接口地址**: `POST /admin/system/clear-cache`

**请求参数**:
```json
{
  "cache_type": "all"  // all-全部，user-用户缓存，cooperation-合作缓存，system-系统缓存
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "缓存清理完成",
  "data": {
    "cleared_items": 156,
    "freed_memory": "45.2MB"
  }
}
```

---

## 6. 数据备份与恢复模块

### 6.1 获取备份列表

**接口地址**: `GET /admin/backups`

**响应示例**:
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "filename": "backup_20240115.sql",
      "size": 102400000,
      "created_at": "2024-01-15 02:00:00",
      "type": "auto",
      "status": "completed"
    },
    {
      "id": 2,
      "filename": "backup_20240114.sql",
      "size": 101200000,
      "created_at": "2024-01-14 02:00:00",
      "type": "manual",
      "status": "completed"
    }
  ]
}
```

---

### 6.2 创建手动备份

**接口地址**: `POST /admin/backups/create`

**响应示例**:
```json
{
  "code": 200,
  "message": "备份任务已创建",
  "data": {
    "task_id": "backup_20240116_143020",
    "estimated_time": "5分钟"
  }
}
```

---

### 6.3 下载备份文件

**接口地址**: `GET /admin/backups/{id}/download`

**响应**: 文件下载流

---

### 6.4 删除备份文件

**接口地址**: `DELETE /admin/backups/{id}`

**响应示例**:
```json
{
  "code": 200,
  "message": "备份文件已删除"
}
```

---

## 7. 权限管理模块

### 7.1 获取管理员列表

**接口地址**: `GET /admin/administrators`

**请求参数**:
- `keyword` (string, 可选): 用户名或邮箱关键词
- `role` (string, 可选): 角色：super_admin-超级管理员，admin-管理员，operator-操作员
- `page` (int, 可选): 页码，默认1
- `size` (int, 可选): 每页数量，默认10

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "username": "admin",
        "email": "admin@edunexus.com",
        "role": "super_admin",
        "status": 1,
        "created_at": "2024-01-01 00:00:00",
        "last_login_time": "2024-01-15 10:30:00"
      },
      {
        "id": 2,
        "username": "operator1",
        "email": "operator1@edunexus.com",
        "role": "operator",
        "status": 1,
        "created_at": "2024-01-05 14:20:00",
        "last_login_time": "2024-01-14 16:45:00"
      }
    ],
    "total": 5,
    "page": 1,
    "size": 10
  }
}
```

---

### 7.2 创建管理员

**接口地址**: `POST /admin/administrators`

**请求参数**:
```json
{
  "username": "operator2",
  "email": "operator2@edunexus.com",
  "password": "Abc123456",
  "role": "operator",
  "permissions": ["user_view", "cooperation_audit"]
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "管理员创建成功",
  "data": {
    "id": 3
  }
}
```

---

### 7.3 更新管理员权限

**接口地址**: `PUT /admin/administrators/{id}/permissions`

**请求参数**:
```json
{
  "permissions": ["user_view", "cooperation_audit", "system_view"]
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "权限更新成功"
}
```

---

## 错误码说明

| 错误码 | 说明 |
|--------|------|
| 200 | 请求成功 |
| 400 | 请求参数错误 |
| 401 | 未授权，需要登录 |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

## 响应格式说明

所有接口统一返回格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

**错误响应示例**:
```json
{
  "code": 400,
  "message": "请求参数错误",
  "errors": {
    "username": ["用户名不能为空"],
    "password": ["密码长度至少8位"]
  }
}
```

---

## 安全说明

1. 所有敏感操作（如删除用户、修改系统配置）需要二次确认
2. 管理员操作会记录详细日志，便于审计
3. 密码采用bcrypt加密存储
4. API接口采用JWT token认证
5. 重要操作支持IP白名单限制