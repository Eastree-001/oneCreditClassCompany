# EduNexus管理员端 - 视频管理数据库表结构设计

## 13. 视频管理表 (videos)

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 说明 |
|--------|------|------|---------|---------|------|
| id | BIGINT | - | 是 | 否 | 视频ID，自增 |
| title | VARCHAR | 200 | 否 | 否 | 视频标题 |
| description | TEXT | - | 否 | 是 | 视频描述 |
| filename | VARCHAR | 200 | 否 | 否 | 视频文件名 |
| file_path | VARCHAR | 500 | 否 | 否 | 视频文件路径 |
| file_size | BIGINT | - | 否 | 否 | 文件大小（字节） |
| duration | INT | - | 否 | 是 | 视频时长（秒） |
| thumbnail | VARCHAR | 500 | 否 | 是 | 缩略图路径 |
| video_type | VARCHAR | 20 | 否 | 否 | 视频类型：course-课程视频，training-培训视频，promotional-宣传视频，other-其他 |
| category | VARCHAR | 50 | 否 | 是 | 视频分类 |
| tags | TEXT | - | 否 | 是 | 标签（JSON格式） |
| status | VARCHAR | 20 | 否 | 否 | 状态：uploading-上传中，processing-处理中，ready-就绪，error-错误 |
| visibility | VARCHAR | 20 | 否 | 否 | 可见性：public-公开，private-私有，internal-内部 |
| uploader_id | BIGINT | - | 否 | 否 | 上传者ID，外键关联administrators.id |
| uploader_name | VARCHAR | 50 | 否 | 否 | 上传者姓名 |
| view_count | INT | - | 否 | 否 | 观看次数，默认0 |
| download_count | INT | - | 否 | 否 | 下载次数，默认0 |
| upload_time | DATETIME | - | 否 | 否 | 上传时间 |
| processed_time | DATETIME | - | 否 | 是 | 处理完成时间 |
| created_at | DATETIME | - | 否 | 否 | 创建时间 |
| updated_at | DATETIME | - | 否 | 是 | 更新时间 |

**索引：**
- PRIMARY KEY (id)
- KEY idx_title (title)
- KEY idx_video_type (video_type)
- KEY idx_status (status)
- KEY idx_visibility (visibility)
- KEY idx_uploader_id (uploader_id)
- KEY idx_upload_time (upload_time)
- KEY idx_category (category)

---

## 14. 视频转码任务表 (video_transcoding_tasks)

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 说明 |
|--------|------|------|---------|---------|------|
| id | BIGINT | - | 是 | 否 | 任务ID，自增 |
| video_id | BIGINT | - | 否 | 否 | 视频ID，外键关联videos.id |
| task_id | VARCHAR | 100 | 否 | 否 | 转码任务ID（唯一） |
| original_file | VARCHAR | 500 | 否 | 否 | 原始文件路径 |
| target_format | VARCHAR | 10 | 否 | 否 | 目标格式：mp4, webm, avi等 |
| resolution | VARCHAR | 20 | 否 | 否 | 分辨率：1080p, 720p, 480p等 |
| status | VARCHAR | 20 | 否 | 否 | 状态：pending-等待中，processing-处理中，completed-完成，failed-失败 |
| progress | INT | - | 否 | 否 | 处理进度（0-100） |
| output_file | VARCHAR | 500 | 否 | 是 | 输出文件路径 |
| error_message | TEXT | - | 否 | 是 | 错误信息 |
| start_time | DATETIME | - | 否 | 是 | 开始时间 |
| end_time | DATETIME | - | 否 | 是 | 结束时间 |
| created_at | DATETIME | - | 否 | 否 | 创建时间 |

**索引：**
- PRIMARY KEY (id)
- UNIQUE KEY uk_task_id (task_id)
- KEY idx_video_id (video_id)
- KEY idx_status (status)
- KEY idx_created_at (created_at)

---

## 15. 视频播放统计表 (video_play_statistics)

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 说明 |
|--------|------|------|---------|---------|------|
| id | BIGINT | - | 是 | 否 | 统计ID，自增 |
| video_id | BIGINT | - | 否 | 否 | 视频ID，外键关联videos.id |
| date | DATE | - | 否 | 否 | 统计日期 |
| play_count | INT | - | 否 | 否 | 播放次数 |
| unique_visitors | INT | - | 否 | 否 | 独立访客数 |
| avg_watch_time | INT | - | 否 | 否 | 平均观看时长（秒） |
| completion_rate | DECIMAL | 5,2 | 否 | 否 | 完成率（%） |
| created_at | DATETIME | - | 否 | 否 | 创建时间 |

**索引：**
- PRIMARY KEY (id)
- UNIQUE KEY uk_video_date (video_id, date)
- KEY idx_video_id (video_id)
- KEY idx_date (date)

---

## 16. 视频评论表 (video_comments)

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 说明 |
|--------|------|------|---------|---------|------|
| id | BIGINT | - | 是 | 否 | 评论ID，自增 |
| video_id | BIGINT | - | 否 | 否 | 视频ID，外键关联videos.id |
| user_id | BIGINT | - | 否 | 否 | 用户ID |
| user_type | VARCHAR | 20 | 否 | 否 | 用户类型：enterprise-企业用户，university-高校用户，admin-管理员 |
| username | VARCHAR | 50 | 否 | 否 | 用户名 |
| content | TEXT | - | 否 | 否 | 评论内容 |
| parent_id | BIGINT | - | 否 | 是 | 父评论ID，支持回复 |
| likes | INT | - | 否 | 否 | 点赞数，默认0 |
| status | VARCHAR | 20 | 否 | 否 | 状态：pending-待审核，approved-已通过，rejected-已拒绝 |
| created_at | DATETIME | - | 否 | 否 | 创建时间 |
| updated_at | DATETIME | - | 否 | 是 | 更新时间 |

**索引：**
- PRIMARY KEY (id)
- KEY idx_video_id (video_id)
- KEY idx_user_id (user_id)
- KEY idx_parent_id (parent_id)
- KEY idx_status (status)
- KEY idx_created_at (created_at)

---

## 数据字典

### 视频类型 (videos.video_type)
- `course`: 课程视频
- `training`: 培训视频
- `promotional`: 宣传视频
- `other`: 其他

### 视频状态 (videos.status)
- `uploading`: 上传中
- `processing`: 处理中
- `ready`: 就绪
- `error`: 错误

### 可见性 (videos.visibility)
- `public`: 公开 - 所有用户可见
- `private`: 私有 - 仅上传者和管理员可见
- `internal`: 内部 - 仅内部用户可见

### 转码任务状态 (video_transcoding_tasks.status)
- `pending`: 等待中
- `processing`: 处理中
- `completed`: 完成
- `failed`: 失败

### 评论状态 (video_comments.status)
- `pending`: 待审核
- `approved`: 已通过
- `rejected`: 已拒绝

---

## 表关系说明

1. **videos** (视频表) 是核心表，存储所有视频文件信息
2. **video_transcoding_tasks** (转码任务) 与 **videos** 是一对多关系，一个视频可以有多个转码任务
3. **video_play_statistics** (播放统计) 与 **videos** 是一对多关系，按日期统计视频播放数据
4. **video_comments** (视频评论) 与 **videos** 是一对多关系，支持评论和回复功能

---

## 功能设计说明

### 1. 视频上传流程
- 支持大文件分片上传
- 支持多种视频格式（MP4, AVI, MOV, WMV等）
- 自动生成缩略图
- 支持视频转码为多种分辨率

### 2. 视频管理功能
- 视频列表查看（支持搜索、筛选、排序）
- 视频信息编辑
- 视频删除（支持批量删除）
- 视频状态管理

### 3. 权限控制
- 不同角色对视频有不同的操作权限
- 支持视频可见性设置
- 评论审核机制

### 4. 数据分析
- 播放次数统计
- 用户观看行为分析
- 视频热度排行

### 5. 安全考虑
- 文件类型验证
- 文件大小限制
- 上传频率限制
- 恶意文件检测